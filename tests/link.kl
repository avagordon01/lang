fn f64 internal(f64 x, f64 y) {
    var z = x;
    var f64 half = 0.5 f64;
    return (z + y) * 0.5f64;
};
export fn f64 average(f64 x, f64 y) {
    var f64 z = 1f64;
    for var i = 0; i < 10; i = i + 1 {
        z = x;
    };
    return internal(z, y);
};
